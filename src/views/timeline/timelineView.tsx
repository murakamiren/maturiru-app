import { VFC } from "react";
import { Text, View } from "react-native";

const TimelineView: VFC = () => {
    return (
        <View>
            <Text>タイムライン</Text>
        </View>
    );
};

export default TimelineView;
